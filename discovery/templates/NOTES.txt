1. Get the Discovery product URL by running the following commands:
     export DISCOVERY_APPLICATION_DOMAIN=$(kubectl \
       --namespace {{ .Release.Namespace }} \
       get route/{{ .Release.Name }} -o "jsonpath={.spec.host}" \
     )
     echo "Visit https://${DISCOVERY_APPLICATION_DOMAIN} to use Discovery."
